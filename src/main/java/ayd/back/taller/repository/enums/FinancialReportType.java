package ayd.back.taller.repository.enums;

public enum FinancialReportType {
        INCOME,   // solo ingresos (payments)
        EXPENSE,  // solo egresos (purchase_orders)
        BOTH      // ambos
}
